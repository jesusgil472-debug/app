services:
  - type: web
    name: scraping-api
    env: python
    region: oregon
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      python -m pyppeteer install
    startCommand: python -m uvicorn scraping:app --host=0.0.0.0 --port=10000





