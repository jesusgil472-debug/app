services:
  - type: web
    name: scraping-api
    env: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install
   startCommand: python -m uvicorn scraping:app --host=0.0.0.0 --port=10000

